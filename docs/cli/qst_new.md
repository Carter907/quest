## qst new

create a new knowledge graph directory

### Synopsis

this command is how you start a new knowledge graph from the command line. You specifiy the name
	of the knowledge graph and a directory will be created with a single guide using default values for
	the frontmatter. These default values can be configured. If you don't specify a directory, the current directory
	will be used.
	Examples:
		qst new learn-cpp

	if you don't specify a directory, the current one will be used:
		qst new

	

```
qst new [flags]
```

### Options

```
  -h, --help   help for new
```

### SEE ALSO

* [qst](qst.md)	 - A lossless compression and formatting tool for knowledge

